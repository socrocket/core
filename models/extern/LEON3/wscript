#! /usr/bin/env python
# vim : set fileencoding=utf-8 expandtab noai ts=4 sw=4 filetype=python :
top = '..'

simulator = [
  'simulatorSources', 
]

def options(ctx): 
  ctx.recurse(simulator)

def configure(ctx):
  ctx.recurse(simulator)

def build(bld):
  bld.recurse(simulator)
